{
  "name": "recipe-app",
  "version": "1.0.0",
  "description": "A clean recipe scraper — ingredients and instructions without the clutter",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
